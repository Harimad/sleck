import React from "react";
import Workspace from "@layouts/Workspace";

const Channel = () => {
  return (
    <Workspace>
      <div>Congraturations For Login!</div>
    </Workspace>
  );
};

export default Channel;
